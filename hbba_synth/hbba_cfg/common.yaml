# Common items for hbba_synth
hbba_conf:

- filtertype:
    name: GenericDivider
    pkg: topic_tools
    # Function signature: func("{module}_{input}_filter", value)
    # A value of 0 always deactivate the filter
    function: "setDividerRate"
